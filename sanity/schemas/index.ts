import homepage from './homepage'
import story from './story'
import { category, event, list, business, authorProfile } from './content'
import { section } from './taxonomy'

export const schemaTypes = [homepage, section, category, story, event, list, business, authorProfile]
